---
title: "Quick start"
description: "Install the CLI, bootstrap with dma init, run dma check, and wire CI."
icon: "Rocket"
---

Minimal path from an empty or existing project to working DMA enforcement. We assume a TypeScript app with `src/` (or you create it via `init`).

Using an AI agent? Copy the prompt — it suggests skill install, `dma init`, and verification steps for your repo.

<AgentPromptButton />

## 1. Install the CLI

```bash
npm install -D @derived-modular/cli
```

## 2. Bootstrap the layout

From the **app package** directory (in a monorepo: `apps/web`, not the workspace root):

```bash
npx @derived-modular/cli init .
```

`init` is **safe on existing projects** — strict skip, never overwrites:

| Creates (if missing) | Skips |
| --- | --- |
| `src/`, `features/`, `shared/` | Existing files and folders |
| `app/` — unless `pages/` or `routes/` already exists | `dma.config.*`, `scripts.dma`, `AGENTS.md` markers |
| `dma.config.ts`, `scripts.dma` in `package.json` | Lint configs |
| DMA block in `AGENTS.md` (append-only) | `services/` |

Resulting tree (minimal):

```text
src/
├── app/          # or pages/, routes/ — composition root for your framework
├── features/
└── shared/
```

Do **not** create `services/` upfront — it appears on the first promotion when **another module** imports the code.

More: [dma init](/tooling/cli/init) · [Project layout](/start/project-layout).

<Callout type="info">
  Prefer manual layout? Create the same folders yourself. Optional `dma.config.*` — [dma.config](/tooling/cli/config).
</Callout>

## 3. Mount a feature from the composition root

The composition root imports modules **only** through `public/`:

```tsx
// src/app/page.tsx
import { CatalogPage } from "@/features/catalog/public/catalog-page";
import { CheckoutPage } from "@/features/checkout/public/checkout-page";

export default function Page() {
  return (
    <div>
      <CatalogPage />
      <CheckoutPage />
    </div>
  );
}
```

A feature may pull `services/*/public/*` and `shared/`, but not other features. Live example — [examples/next-app](https://github.com/mikhailmogilnikov/derived-modular-architecture/tree/main/examples/next-app).

## 4. Run the check

```bash
npx @derived-modular/cli check .
```

Single app with `src/` — one graph. Monorepo root without `src/` — the CLI discovers apps (or use `--roots`). Each root is a **separate** graph.

Typical first errors:

| Rule | What happened | What to do |
| --- | --- | --- |
| `public-api` | Import into `features/foo/model`, not `public/` | Export the symbol in `public/` or import from there |
| `feature-to-feature` | Feature A imports feature B | Promote to `services/` or extract to `shared/` (second use) |
| `layer-direction` | Import “upward”, e.g. from `shared` into `features` | Reverse the dependency |

All rules — [dma check](/tooling/cli/check). Monorepo — [Monorepo guide](/guides/monorepo).

## 5. Add to CI

```bash
npx @derived-modular/cli check . --format json
```

More — [CI](/tooling/ci).

## 6. (Optional) Linter in the editor

Fast feedback on save — **not** a substitute for `dma check`:

```bash
npm install -D @derived-modular/eslint-plugin eslint
```

```js
// eslint.config.js
import dma from "@derived-modular/eslint-plugin";

export default [
  ...dma.configs.recommended,
  {
    settings: {
      dma: {
        srcRoot: "src",
        compositionRoots: ["app", "pages", "routes"],
      },
    },
  },
];
```

ESLint can soft-load `srcRoot` / `compositionRoots` from `dma.config.*` when `settings.dma` is unset. Setup — [ESLint](/tooling/eslint).

## 7. Local growth signals

```bash
npx @derived-modular/cli doctor .
```

`doctor` does not fail CI (exit 0) but hints when to extract to `shared`, split segments, or promote a feature to a service. More — [dma doctor](/tooling/cli/doctor).

## What's next

<Cards>
  <Card title="Four invariants" description="Rules that hold the architecture" href="/concepts/invariants" />
  <Card title="Modules & public API" description="Module growth stages" href="/concepts/modules" />
  <Card title="dma init" description="Bootstrap layout and AGENTS.md" href="/tooling/cli/init" />
  <Card title="AI & agents" description="dma skill for Cursor" href="/tooling/ai-agents" />
</Cards>
